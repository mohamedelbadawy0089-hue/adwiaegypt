# كيفية استخدام دالة معالجة اللصق السريع مع (اسم المنتج + رقم التشغيلة)

## الملفات المطلوبة

1. **SQL Function**: `batch_process_drugs_with_batch_number.sql`
2. **JavaScript Function**: `batch_process_with_batch_number.js` (مدمجة في stock.html)
3. **التعديل في**: `stock.html` (تم تحديث زر الحفظ لاستخدام الدالة الجديدة)

## خطوات التثبيت

### 1. تثبيت دالة SQL في Supabase

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. سجل الدخول واختر مشروعك (slamtak)
3. من القائمة الجانبية، اختر "SQL Editor"
4. انقر على "New Query"
5. انسخ محتوى ملف `batch_process_drugs_with_batch_number.sql`
6. الصقه في SQL Editor
7. انقر على "Run" أو اضغط `Ctrl+Enter`

### 2. التحقق من التثبيت

بعد التنفيذ، تأكد من ظهور رسالة نجاح وأن الدالة تم إنشاؤها بدون أخطاء.

## الشروط التي تعمل بها الدالة

### المفتاح الأساسي
- **مركب من**: (اسم المنتج + رقم التشغيلة)
- **الصيغة**: `tradeName + '|' + batchNumber`

### سيناريو 1: تطابق نفس التشغيلة
- **الشرط**: المنتج موجود بنفس `tradeName` و `batchNumber`
- **الإجراء**: 
  - تحديث الأسعار (السعر، خصم البيع، خصم الشراء)
  - تحديث التواريخ (الإنتاج، الانتهاء)
  - **جمع الكمية الجديدة على الكمية القديمة**
  - تحديث `updated_at`

### سيناريو 2: تشغيلة مختلفة
- **الشرط**: المنتج موجود ولكن `batchNumber` مختلف
- **الإجراء**: إضافة سطر جديد كمنتج مستقل تماماً

### سيناريو 3: منتج جديد
- **الشرط**: المنتج غير موجود مسبقاً
- **الإجراء**: إضافة سطر جديد

## المميزات

### الأداء
- **حجم الدفعة**: 500 منتج لكل دفعة
- **عدد الدفعات لـ 20,000 منتج**: 40 دفعة فقط
- **معالجة غير متزامنة**: لا تجميد للمتصفح
- **فواصل زمنية**: 200ms بين الدفعات

### الاستقرار
- **Exponential Backoff**: إعادة المحاولة التلقائية في حال فشل الاتصال
- **معالجة الأخطاء**: تسجيل الأخطاء دون إيقاف العملية بالكامل
- **إحصائيات**: تتبع عدد المنتجات المضافة والمحدثة والمتجاهلة

### الدقة
- **معالجة التواريخ**: دعم صيغ متعددة (MM/YYYY, MM-YYYY, DD-MM-YYYY)
- **جمع الكميات**: منطق رياضي دقيق لجمع الكميات
- **تحديث شامل**: تحديث جميع الحقول المالية والتاريخية

## الاستخدام

### 1. لصق البيانات من Excel
1. انسخ البيانات من Excel (تأكد من وجود أعمدة: اسم المنتج، رقم التشغيلة، الكمية، السعر، خصم البيع، إلخ)
2. الصق البيانات في منطقة اللصق السريع في stock.html
3. سيتم عرض معاينة للبيانات

### 2. حفظ البيانات
1. اضغط على زر "💾 حفظ البيانات"
2. سيتم معالجة البيانات في دفعات من 500 منتج
3. شريط التقدم يوضح حالة المعالجة
4. رسالة نهائية توضح عدد المنتجات المضافة والمحدثة

### 3. فحص النتائج
- يتم تحديث الجدول تلقائياً بعد اكتمال الحفظ
- المنتجات بنفس التشغيلة سيتم دمج كمياتها
- المنتجات بتشغيلة مختلفة ستظهر كأسطر منفصلة

## معالجة الأخطاء

### أخطاء الاتصال
- يتم إعادة المحاولة تلقائياً حتى 3 مرات
- وقت الانتظار يتضاعف مع كل محاولة (Exponential Backoff)

### أخطاء البيانات
- يتم تسجيل الأخطاء في المصفوفة `errors`
- لا توقف أخطاء فردية العملية بالكامل
- يتم عرض تقرير نهائي بالأخطاء

### فشل الدفعات
- إذا فشلت 3 دفعات متتالية، تتوقف العملية
- يتم عرض رسالة خطأ للمستخدم
- يمكن إعادة المحاولة بعد إصلاح المشكلة

## المثال العملي

### البيانات الملصوقة
```
tradeName     | batchNumber | quantity | price | discount
--------------|-------------|----------|-------|----------
Panadol       | ABC123      | 100      | 10    | 15
Panadol       | ABC123      | 50       | 12    | 20
Panadol       | XYZ789      | 75       | 10    | 15
Aspirin       | DEF456      | 200      | 8     | 10
```

### النتيجة المتوقعة
1. **Panadol ABC123**: سطر واحد مع كمية = 150 (100 + 50) والسعر المحدث = 12
2. **Panadol XYZ789**: سطر جديد (تشغيلة مختلفة)
3. **Aspirin DEF456**: سطر جديد

## الفهارس المحسّنة

تم إنشاء فهرس GIN لتحسين أداء البحث على المفتاح المركب:
```sql
CREATE INDEX idx_warehouse_products_composite_key 
ON warehouse_products_flexible USING GIN (
    (product_data->>'tradeName'),
    (product_data->>'batchNumber')
);
```

## الدعم

في حال وجود مشاكل:
1. راجع Console في المتصفح للسجلات
2. تأكد من تثبيت دالة SQL بشكل صحيح
3. تحقق من صحة البيانات الملصوقة
4. تأكد من وجود `batchNumber` في البيانات